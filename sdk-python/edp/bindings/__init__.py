"""Framework-specific bindings for EDP.

Each submodule is an optional extra. Install via:
    pip install explicit-decision-protocol[langgraph]
    pip install explicit-decision-protocol[pydantic-ai]
"""
